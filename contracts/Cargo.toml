[package]
name = "civictrust-contracts"
version = "0.1.0"
edition = "2021"

[dependencies]
casper-contract = "4.0"
casper-types = "4.0"

[lib]
crate-type = ["cdylib"]
path = "src/lib.rs"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
